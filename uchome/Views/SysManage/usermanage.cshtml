<link rel="stylesheet" href="@Url.Content("~/plugs/datatables/css/jquery.dataTables.css")">
<link rel="stylesheet" href="@Url.Content("~/plugs/datatables/css/dataTables.bootstrap.css")">
<link rel="stylesheet" href="@Url.Content("~/plugs/scrollbar/css/sui.css")">
<script src="@Url.Content("~/plugs/datatables/js/jquery.datatables.js")"></script>
<script src="@Url.Content("~/plugs/datatables/js/dataTables.bootstrap.js")"></script>
<script src="@Url.Content("~/plugs/datatables/js/Jquery.dataTableSetting.js")"></script>

<style>
    .red {
        color: red;
    }

    .green {
        color: green;
    }

    #ul_appicon {
    }

        #ul_appicon > li {
            position: relative;
        }

            #ul_appicon > li input[type=checkbox] {
                position: absolute;
                right: 0;
                bottom: 0;
                border: solid 1px #ccc !important;
            }
</style>
<div id="sysuserlist" class="form-position pagew">
    <form class="form-inline">
        <div class="form-group">
            <label for="exampleInputName2">姓名：</label>
            <input type="text" class="form-control" id="xm" placeholder="请输入用户姓名">
        </div>
        <div class="form-group">
            <label for="exampleInputEmail2">帐号：</label>
            <input type="text" class="form-control" id="username" placeholder="请输入用户帐号">
        </div>
        <input type="button" class="btn btn-info" id="btn-searchuser" value="模糊查询">
        <table class="table table-striped table-bordered table-hover datatable" id="table_users">
            <thead>
                <tr>
                    <th>用户姓名</th>
                    <th>登录帐号</th>
                    <th>用户类型</th>
                    @*<th>联系邮箱</th>*@
                    @*<th>联系电话</th>*@
                    <th>身份识别号</th>
                    <th>用户状态</th>
                    <th>操作</th>
                </tr>
            </thead>
            @*<tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.XM</td>
                            <td>@item.UserName</td>
                            <td>@item.UserType</td>
                            <td>@item.UserEmail</td>
                            <td>@item.UserMobile</td>
                            <td>@(item.UserType.ToLower() == "t" ? item.SFZH : item.XJH)</td>
                            <td>@(item.State == 1 ? "正常" : "已禁用")</td>
                        </tr>
                    }
                </tbody>*@
            <tfoot>
                <tr><th colspan="6">当前显示前100项记录，共<label id="records">@ViewBag.Records</label> 项，如未找到你想要的结果，可以将筛选条件调整更精确！</th></tr>
            </tfoot>
        </table>
    </form>
</div>
<script>
    var initlisturl = "@Url.Action("GetUserList")";
    var operurl_edit = "@Url.Action("ResetPwd")";
    var operurl_delete = "@Url.Action("DeleteUser")";
    var operurl_recovery = "@Url.Action("recoveryUser")";
</script>
<script src="@Url.Content("~/angular/uchome/sysmanage/usermanage.js")"></script>
