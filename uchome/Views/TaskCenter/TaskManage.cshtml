<link rel="stylesheet" href="@Url.Content("~/plugs/datatables/css/jquery.dataTables.css")">
<link rel="stylesheet" href="@Url.Content("~/plugs/datatables/css/dataTables.bootstrap.css")">
<link rel="stylesheet" href="@Url.Content("~/plugs/scrollbar/css/sui.css")">
<script src="@Url.Content("~/plugs/datatables/js/jquery.datatables.js")"></script>
<script src="@Url.Content("~/plugs/datatables/js/dataTables.bootstrap.js")"></script>
<script src="@Url.Content("~/plugs/datatables/js/Jquery.dataTableSetting.js")"></script>

<style>
    .red {
        color: red;
    }

    .green {
        color: green;
    }

    #ul_appicon {
    }

        #ul_appicon > li {
            position: relative;
        }

            #ul_appicon > li input[type=checkbox] {
                position: absolute;
                right: 0;
                bottom: 0;
                border: solid 1px #ccc !important;
            }
</style>
<div id="sysuserlist" class="form-position pagew">
    <form class="form-inline">
        <div class="form-group">
            <label for="exampleInputName2">任务名称：</label>
            <input type="text" class="form-control" id="xm" placeholder="请输入任务名称">
        </div>
        <input type="button" class="btn btn-info" id="btn-searchuser" value="模糊查询">
       
        <table class="table table-striped table-bordered table-hover datatable" id="table_task">
            <thead>
                <tr>
                    <th>任务名称</th>
                    <th>任务链接</th>
                    <th>任务状态</th>
                    <th>操作</th>
                </tr>
            </thead>
            @*<tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.XM</td>
                            <td>@item.UserName</td>
                            <td>@item.UserType</td>
                            <td>@item.UserEmail</td>
                            <td>@item.UserMobile</td>
                            <td>@(item.UserType.ToLower() == "t" ? item.SFZH : item.XJH)</td>
                            <td>@(item.State == 1 ? "正常" : "已禁用")</td>
                        </tr>
                    }
                </tbody>*@
        </table>
        <div>当前显示前100项记录，共<label id="records">@ViewBag.Records</label> 项，如未找到你想要的结果，可以将筛选条件调整更精确！</div>
    </form>
</div>
<script>
    var getalltaskurl = "@Url.Action("GetAllTask")";
    var url_taskcondition = "@Url.Action("TaskCondition")";
    var url_tasktrigger = "@Url.Action("TaskTrigger")";
    var url_tasktarget = "@Url.Action("TaskTarget")";
    var url_taskreward = "@Url.Action("TaskReward")";
</script>
<script src="@Url.Content("~/angular/uchome/taskcenter/taskmanage.js")"></script>
